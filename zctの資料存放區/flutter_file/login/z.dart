import 'package:flutter/material.dart';

class AssistantPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Assistant Page'),
      ),
      body: Center(
        child: Text('Welcome, Assistant!'),
      ),
    );
  }
}
