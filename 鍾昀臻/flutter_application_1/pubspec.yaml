name: flutter_application_1
description: A new Flutter application.

# The following defines the version and build number for your application.
version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  file_picker: ^4.5.0  # Add the latest version available on pub.dev

dev_dependencies:
  flutter_test:
    sdk: flutter

# To add assets to your application, add an assets section, like this:
flutter:
  #assets:
   # - images/a_dot_burr.jpeg
    #- images/a_dot_ham.jpeg

  # To add fonts to your application, add a fonts section here, in this "flutter" section.
  # Each entry in this list should have a "family" key with the font family name, and a
  # "fonts" key with a list giving the asset and other descriptors for the font. For example:
  fonts:
    - family: Raleway
      fonts:
        - asset: fonts/Raleway-Regular.ttf
